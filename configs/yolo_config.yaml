# YOLOv8 Training Hyperparameters Configuration
task: detect
mode: train
model: yolov8n.pt  # pre-trained nano backbone
data: configs/dataset.yaml
epochs: 100
batch: 16
imgsz: 640
save: true
device: 0  # GPU index (use 'cpu' if no GPU available)
workers: 8
project: models/train
name: aquascan_yolov8
optimizer: AdamW
lr0: 0.01
lrf: 0.01
val: true
cache: true
